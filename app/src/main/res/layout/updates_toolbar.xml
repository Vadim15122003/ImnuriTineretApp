<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="15dp"
    android:gravity="center_vertical"
    android:id="@+id/toolbar_updates"
    android:background="@color/deafult_toolbar">
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="ClickMenu"
        android:src="@drawable/ic_menu"
        android:id="@+id/updatestes_icon"
        android:background="@drawable/buton_press_button_toolbar"
        />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="@string/toolbar_updates_text"
        app:autoSizeTextType="uniform"
        android:textColor="#ffffff"
        android:textStyle="bold"
        android:textAlignment="center"
        android:id="@+id/title_updates"
        />

</LinearLayout>
